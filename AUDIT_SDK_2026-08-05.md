# QZDB 多语言 SDK 审计报告（2026-08-05）

审计范围：`multi-lang/` 下 8 个分发版 SDK（C / Go / Java / PHP / Node.js / Rust / Python / netcore）+ `TestRunner/QzdbSearcher.cs`（V18 真值校验器副本）。
基准规范：仓库根 `FORMAT.md`、各 SDK 源码、已修复的 `REPORT_asn_china_56554.md`。

---

## 一、解析正确性结论

**总判定：当前所有 SDK 解析逻辑正确。** 上一轮的 Bug1（ROW_SCHEMA 偏移）已在 9 个文件修复；Bug2（哨兵位泄漏）是 Python 特有并已修复，其余语言本就正确（故 Node.js / C 真实数据回归 100%）。

逐项核对（均通过）：

| 检查项 | 结果 | 证据 |
|--------|------|------|
| 哨兵位剥离（`& SENTINEL_MASK_31`） | ✅ 全 8 语言正确 | Python 982/992/1001；C 656/668/696/711；Go 670/685/709/729；Java 508/520/532/546；PHP 617/632/669/689；Node 594/610/640/649；Rust 777/796/816/830；netcore 589/602/615/628 |
| 查找函数统一剥离（walk 内部每轮剥离，返回值干净） | ✅ 正确 | C `trie_walk_v4` 主循环 668 行对 24/32-bit 路径统一剥离；其余 SDK 同构 |
| ROW_SCHEMA 规范布局（f_count=sp+0 / stride=sp+1 / 字段循环 sp+4） | ✅ 9 文件已修 | netcore 643-673；C/Go/Java/PHP/Node/Rust/Python/TestRunner 均已对齐权威 `QZDBReader.cs` |
| node_size 双模式（flags 0x10/0x20 → 6 或 8 字节） | ✅ 全 8 语言一致 | C 158-159；Python 427-428；其余 SDK 同 |
| PoolIdxSize 2/3 字节 | ✅ 全 8 语言校验 `must be 2 or 3` | Go 266-268；C 168-169；其余同 |
| V6JumpBits 动态（默认 16） | ✅ 全 8 语言读取 d[11] | Go 257-262；C 161-163；其余同 |
| v4/v6 路由（按 IP 位宽自动分派） | ✅ 正确 | Python `find_uint`/`find_v6_uint`；其余同 |
| Metadata / GROUP_SCHEMA / field_names 动态读取 | ✅ 全 8 语言优先 Metadata，硬编码仅回退 | Python 624-653；Rust 596-637；其余同 |

> 注：Bug2 之所以只在 Python 暴露、其他语言本来正确，是因为其他语言的 `find` 在 walk 内部已统一剥离；Python 旧版的 `_resolve_row_id` 漏剥离 leaf 返回值，已修。

---

## 二、版本兼容性（用户重点问项）

**结论：8 个 SDK 对"不同版本"的解析逻辑完全一致且正确，但文档与实现严重矛盾，且存在维护炸弹。**

- 所有 SDK 读 `d[4]` 为 `fmt_ver`，统一接受 **1–6**，使用**完全相同**的硬编码分支：
  - `if (fmt_ver == 1 || fmt_ver >= 4)` → `entry_count` 用 u32，否则 u16
  - `if (fmt_ver == 1 || fmt_ver >= 3)` → `dim_mask` 用 u16，否则硬编码 `0x01 if gi!=2 else 0x02`
- 真实 `asn_china` 是 v4+ 文件（含动态宽度 + dim_mask），所有 SDK 解析一致 → 跨语言结果可一致。

**⚠️ 文档与实现矛盾（高优先级文档问题）：**
1. `FORMAT.md` §10.1 写："读取 HeaderVersion；`QZDBReader` 接受 `1`（`fmtVer != 1` 抛 `NotSupportedException`）"。**实际实现接受 1–6**，文档严重滞后，会误导新人按 v1-only 写新 SDK。
2. `FORMAT.md` §4.2 写 V6JumpBits 范围 **8~20**；**所有 SDK 校验 16~20 并拒绝 <16**。若真实存在 V6JumpBits=8~15 的文件（文档允许），所有 SDK 会误拒。

---

## 三、漏洞清单（按严重性）

### 🔴 高
1. **CRC 默认不校验（违反 §10.6）**
   - 所有 SDK 都实现了 `verify_crc()`，但 **init 阶段不自动调用**（需用户手动调）。
   - §10.6 明确要求"Reader 默认在打开时校验；生产路径应保持开启"。
   - 风险：文件截断/损坏/版本错配时，SDK 不会默认报错，可能静默返回错误数据（这正是客户"解析不对"类问题的温床）。
   - 建议：在 `init`/`open` 增加 `verifyCrc` 默认参数（默认 true），失败抛 `Corrupted` 异常。

2. **V6JumpBits 实现拒绝文档允许的值（8–15）**
   - 代码：`if (v6JumpBits < 16 || > 20) throw`。文档允许 8–20。
   - 风险：合法文件被误拒；或文档应改为 16–20（需先确认构建器实际产出范围）。
   - 建议：以构建器实际产出为准对齐文档与代码，二者必须一致。

### 🟠 中
3. **硬编码版本分支 + dim_mask 硬编码回退（违反 §10.2）**
   - 8 个 SDK 各自重复 `fmt_ver==1||>=4` / `==1||>=3` 分支，且 dim_mask 回退 `0x01 if gi!=2 else 0x02`。
   - §10.2 明确："SDK 不应硬编码 groupIndex → 版本关系，应通过 Metadata 读取。"
   - 风险：新增格式版本须改 8 处同一逻辑，极易漏改导致**跨语言解析不一致**（最危险的长期隐患）。
   - 建议：所有版本/字段宽度/维度映射一律从 Metadata 动态解析，删除 `fmt_ver` 硬编码分支。

4. **dim_mask 回退假设脆弱**
   - 硬编码 `gi!=2 ? 0x01 : 0x02` 仅在"group2=asn"约定下成立。若某文件 group 布局不同（多 group、asn 不在 group2），回退会解析错。
   - Metadata 存在时应以 Metadata 为准（已实现），但该回退分支本身是错误的兜底。

### 🟡 低 / 构建
5. **Go `main redeclared`**：`main.go` 与 `batch_main.go` 同包重复声明 `main`（预先存在，与本轮修改无关）。阻碍 Go 端到端真实数据回归。
6. **FORMAT.md 文档整体滞后**：§10.1（版本）、§4.2（V6JumpBits）与实际不符，文档不可信。

---

## 四、缺失的需求

1. **CRC 默认校验**（§10.6 未落实）
2. **动态版本/字段映射**（全走 Metadata，消除 `fmt_ver` 硬编码）—— §10.2/10.3 未落实
3. **V6JumpBits 支持文档下限 8**（代码目前拒绝 <16，需与构建器对齐）
4. **跨语言真实数据端到端回归**：目前仅 Python / Node.js / C 跑了 `asn_china` 106103 行；**Go / Java / PHP / Rust 仅语法/编译检查，未跑真实数据**
5. **asn / pro 版本测试数据**：`tools/test_cases/` 只有 std / ult / max，**缺 asn（正是出问题的版本！）、pro**
6. **跨 node_size 测试**：需 node_size=6 与 node_size=8 两种文件各一份验证（目前仅覆盖一种）
7. **PoolIdxSize=3 测试**：需 string 池条目 > 65535 的文件
8. **CRC 失败注入测试**：构造损坏文件，验证 `verify_crc()` 能捕获
9. **格式版本矩阵测试**：fmt_ver=1/2/3/4/5/6 各一份样本做回归

---

## 五、不必要的复杂性

1. **（最大）8 个 SDK 各自硬编码相同的版本/格式分支**
   - `fmt_ver` 判断、`dim_mask` 回退在 8 处完全重复。应提取为**一份共享规范**（或一个代码生成器/模板），从单一真相生成，杜绝漂移。
   - 这也是"漏洞 #3"的根因：复制粘贴式维护必然导致跨语言不一致。

2. **dim_mask 硬编码回退分支** 可直接从 Metadata 读取消除。

3. **C 头文件大量 `field_names` 重载 API**（`const char**` 多签名，q命令.h 144/149/152）——可收敛为单一投影接口。

4. **Python `find_uint` 对 walk 返回值冗余 `& SENTINEL_MASK_31`**（982 行）：walk 已剥离，冗余。无害但增加认知负担，且暗示"walk 可能返回带哨兵"，易误导后续维护者。建议统一约定"walk 返回干净 row_id"，移除调用处冗余剥离（或反之统一在调用处剥离、walk 返回带哨兵——但需全语言统一）。

5. **两套哨兵常量**（`SENTINEL` / `SENTINEL_MASK_24` / `SENTINEL_MASK_31`）：设计上必要（jump 始终 32 位哨兵；trie 内部按 node_size 选 24/32 位），但命名易混。建议加醒目注释："jump 表值恒定 32 位；trie 节点按 node_size 选 24/32 位"。

---

## 六、缺失的测试

| 缺失测试 | 重要性 | 现状 |
|----------|--------|------|
| **asn 版本** test_cases（asn_china / asn_global） | 🔴 高（出问题的是 asn） | 缺失，仅上一轮临时做了 106103 行回归，未沉淀为常态化用例 |
| **pro 版本** test_cases | 🟠 中 | 缺失 |
| **node_size=6 专项**（24-bit trie 路径） | 🔴 高（Bug2 潜伏区） | 缺失；当前验证文件多为 32-bit，24-bit 路径未经真实数据考验 |
| **PoolIdxSize=3 专项**（大 string 池） | 🟠 中 | 缺失 |
| **CRC 损坏注入**（截断/翻转字节） | 🔴 高（验证 #1 漏洞是否真能捕获） | 缺失 |
| **格式版本矩阵**（fmt_ver 1–6） | 🟠 中 | 缺失 |
| **Go / Java / PHP / Rust 真实数据回归** | 🔴 高 | 缺失（仅 Python/Node/C 验证过 100%） |
| **跨语言一致 CI**（cross_verify 常态化） | 🟠 中 | `cross_verify.py` 存在但依赖临时构建，未入 CI |
| **边界/溢出**（verify_boundary 覆盖各 SDK） | 🟡 低 | `verify_boundary.py` 仅 Python 引用 |

---

## 七、建议优先级

1. **P0（立即）**：落实 CRC 默认校验（漏洞 #1）；对齐 V6JumpBits 文档与代码（漏洞 #2）。
2. **P1（本周）**：补 asn / pro 版本 test_cases；补 node_size=6 与 PoolIdxSize=3 真实文件测试；补 CRC 损坏注入测试；跑通 Go/Java/PHP/Rust 真实数据回归（先修 Go `main redeclared`）。
3. **P2（重构）**：删除 8 处 `fmt_ver` 硬编码分支，改为全 Metadata 驱动；收敛 C 冗余 API；统一哨兵剥离约定（建议 walk 返回干净 row_id，调用处不再剥离）。
4. **P3（文档）**：重写 FORMAT.md §10.1（版本接受范围）、§4.2（V6JumpBits 范围），使其与实现一致。

---

> 附：本次审计确认——上一轮 asn_china 56554 崩溃的根因（Bug2 哨兵泄漏）**仅 Python 受影响且已修**；Bug1（ROW_SCHEMA）已在 9 文件修复。当前所有 SDK 解析正确，但**测试覆盖与版本/CRC 工程化**存在系统性缺口，是后续最高优先级工作。
