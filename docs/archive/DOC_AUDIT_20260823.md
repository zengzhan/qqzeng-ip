# QZDB SDK 文档质量审计报告

**日期**: 2026-08-23 · **范围**: 面向用户的全部文档（根 README、multi-lang/README、8 语言 README、docs/ 规范文档）
**标准**: 简洁 · 高效 · 明了 —— 开发者 5 分钟内完成首次集成；示例代码与实现零偏差；同一信息全仓只有一处权威来源。
**执行状态**: 第一批（P0+P1）已于 2026-08-23 执行完毕（A1/A2/A3/A4/B1/B2/C3/D2 + SYNC_GUIDE §六）；第二批 P2 待批准。附录 S1 安全事项已核实关闭（测试数据，无需处置）。

---

## 一、总评

SDK 本体的工程质量（8 语言同构 API、四层验证体系）已达到商业级水准，但**文档层明显滞后于代码层**。当前状态可概括为三点：门面失守（根目录堆放 9 份过程稿、README 存在事实错误）、示例失真（至少 1 处编译不过、多处违反自家 fail-closed 原则）、风格失序（emoji 与营销化措辞不均、8 份语言文档结构不同构）。以下按严重度分级列出，共 14 项。

---

## 二、发现清单

### P0 — 硬伤：错误或失效，直接损害可信度

| # | 位置 | 问题 | 改法 |
|---|------|------|------|
| A1 | `README.md:31` | 写"包含 **64 字节 Header 结构**"。权威规范（FORMAT §3）明确 Header 固定 **192 字节**；64 字节是另一概念（段对齐 Align64）。两者被混为一谈 | 改为"192 字节 Header"，与 FORMAT.md 逐字一致 |
| A2 | `README.md:12-24` | 结构树过时：写 `csharp/` 实际目录是 `netcore/`；缺 `demo/`、`cmd/`、`tools/`、`sql/`；验证脚本只列 `run_all_tests.sh`，漏掉 CLAUDE.md 认定的统一编排器 `run_all.sh` | 以 `ls` 实际输出重画结构树 |
| B1 | `multi-lang/README.md:86` | Rust 快速入门**编译不过**：`from_file` 返回 `Result<QzdbReader, QzdbError>`（lib.rs:2369），示例直接当值调用 `.find()` | 补 `match`/`?` 解包；所有语言示例纳入 CI 片段校验 |
| B2 | `multi-lang/README.md:116-117` | C 示例三连违规：`buf[256]` 会静默截断定点展开输出（§10.5 最长约 309 字符 + NUL）；不检查返回码；不调 `qzdb_close`。示范代码违反项目自己的 fail-closed 安全规则 | 用 `QZDB_VALUE_BUF_SIZE`、检查返回码、补 close |

### P1 — 应改：影响专业观感与集成效率

| # | 位置 | 问题 | 改法 |
|---|------|------|------|
| A3 | 仓库根目录 | `CODE_AUDIT_REPORT.md`、`REVIEW_9a7e8a6.md`、`PATCH_REVIEW_20260810.md`、`RELEASE_READINESS_REPORT.md` 等 **9 份过程性工作稿**全部被 git 跟踪并占据根目录。SDK 根目录应是产品门面，不是工作台 | 移入 `docs/archive/` 或直接删除（内容已被 ROADMAP 吸收的均可删） |
| A4 | 两份 README | 职责倒置：根 README 无一行可运行代码；真正的产品主页内容（能力介绍、8 语言快速入门）在 `multi-lang/README.md`。开发者落地路径断裂 | 根 README 承担产品门面（一句话定位 + 最小示例 + 文档索引）；multi-lang README 收敛为多语言细节手册 |
| C3 | `QZDB_SDK_API.md:5-6` | 正式规范的头部写着"真·全量无损合并版"、"v2.3 自称…但实际静默删除…"——网络用语 + 内部审计叙事进入规范正文头 | 头部只留版本号/日期/范围；修订史归附录 B/C |
| D2 | `README.md:49` | 各语言"使用方式"描述与实际不符处：Rust 写"拷贝 `src/lib.rs` + Cargo.toml 依赖"，实际是整个 crate 目录；Go 的 import 路径写的是占位符 | 逐语言核对后修正，或直接链接到各语言 README 的对应章节 |

### P2 — 可选：风格统一与长期可维护性

| # | 位置 | 问题 | 改法 |
|---|------|------|------|
| B3 | `multi-lang/README.md:61` | Go 示例用内置 `println`（输出到 stderr，仅调试用） | 改 `fmt.Println` |
| C1 | 全仓 | emoji 使用三种标准并存：multi-lang/README 23 个、php/README 4 个、java/README 2 个、其余 0 个 | 统一为零 emoji（技术文档惯例），或制定使用规范；推荐前者 |
| C2 | multi-lang/README、API 规范 | 营销化措辞："极速"、"前缀压路机压缩"、"极具 CPU 缓存友好性" | 换成可验证的技术陈述（数据结构名 + 实测数字） |
| C4 | multi-lang/README ×8 处 | LaTeX 记号 `$\mathcal{O}(…)$` 在 GitHub 可渲染，但 VS Code 默认预览等环境显示原始代码 | 改纯文本 `O(W−K)` |
| C5 | multi-lang/README §对比表 | 点名友商格式且自称"客观对比"，措辞带倾向性 | 保留表格但中立化行文，加"以公开资料为准"限定 |
| D1 | 8 语言 README | 行数 347~726、章节数 23~48，结构不同构；SYNC_GUIDE 只约束代码同步不约束文档同步，漂移会持续扩大 | 制定统一章节骨架（快速开始/加载/查询/错误处理/并发/API 表），写入 SYNC_GUIDE 并作为 L2 检查项 |

---

## 三、整改建议

**第一批（P0+P1，约 7 个文件）**：改根 README（A1/A2/A4）、清理根目录工作稿（A3）、修 Rust 与 C 示例（B1/B2）、规范头部去叙事化（C3）、使用方式表纠偏（D2）。全部可在本次会话内完成并有 git diff 可审。

**第二批（P2）**：风格统一涉及 10 个文件的全篇措辞，建议单独一批执行，便于 review。

**防复发机制**（随第一批落地）：SYNC_GUIDE 增补"文档同步"节——API 签名变更须同步检查 8 语言 README + 两份总 README 中的示例片段；有条件时把各 README 的代码块抽取为可编译片段纳入 L1 冒烟。

---

## 四、目标形态（根 README 骨架示意)

```
# qzdb — IP 地理位置查询 SDK
一句话定位 + 关键指标（格式版本 / 8 语言 / 查询复杂度）
## 快速开始（任选一语言 ≤10 行可运行示例）
## 安装（指向各语言 README 的锚点表）
## 文档（FORMAT / SDK_API / SYNC_GUIDE 三链接）
## 仓库结构（实测目录树）
## License
```
其余内容一律下放，单一来源原则：每个事实只在一处维护。

---

## 附录：整改中新发现的待决策事项

**S1 · `demo/qqzeng-ip-ult.qzdb`（4.5 MB）被 git 跟踪** —— 已关闭（2026-08-23）：经确认该文件为测试用途数据，非购买数据副本，保留现状，无需处置。
